<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            stylesheets="@dark-theme.css"
            fx:controller="at.ac.hcw.allesinordnung.controller.CollectionController">

    <!-- TOP leer lassen, damit nichts Platz wegnimmt -->
    <top>
        <Pane minHeight="0"/>
    </top>

    <!-- CENTER enthält: Spacer (drückt nach unten) + Titel/Suche/Menu -->

    <center>
        <VBox spacing="0">

            <!-- fester Versatz: wie weit "unterhalb" beginnen (anpassen!) -->
            <Region minHeight="40"/>

            <!-- flexibler Spacer: nimmt den übrigen Platz über dem Inhalt -->
            <Region VBox.vgrow="ALWAYS"/>

            <!-- Inhalt: Titel links + rechts daneben Suche -->
            <HBox spacing="24" alignment="TOP_LEFT">
                <padding>
                    <Insets top="0" right="24" bottom="24" left="24"/>
                </padding>

                <!-- Linke Spalte: großer Titel + Menü -->
                <VBox spacing="24" alignment="TOP_LEFT">
                    <children>
                        <Label text="Willkommen :)" styleClass="welcome-title"/>
                        <VBox spacing="16" styleClass="welcome-menu">
                            <children>
                                <Button text="Bücher"/>
                                <Button text="DVDs"/>
                                <Button text="CDs"/>
                                <Button text="ganzes Verzeichnis"/>
                                <Button text="Datei erstellen"/>
                            </children>
                        </VBox>
                    </children>
                </VBox>

                <!-- Mittel-Spacer schiebt die rechte Spalte wirklich nach rechts -->
                <Pane HBox.hgrow="ALWAYS"/>

                <!-- Rechte Spalte: Suche (oben am Titel ausgerichtet) -->
                <VBox spacing="8" alignment="TOP_RIGHT">
                    <children>
                        <TextField promptText="Suchen..." styleClass="text-field" prefWidth="260"/>
                        <Button text="🔎" styleClass="icon-btn"/>
                    </children>
                </VBox>
            </HBox>

            <!-- kleiner unterer Rand, damit nichts am Fenster klebt -->
            <Region minHeight="24"/>
        </VBox>
    </center>


    <!-- BOTTOM optional leer -->
    <bottom>
        <Pane minHeight="0"/>
    </bottom>

</BorderPane>
